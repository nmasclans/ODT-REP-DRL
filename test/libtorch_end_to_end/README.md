## Compile, Generate application executable and Run

To compile and create an executable, open terminal in this directory and use CMake compiler:
```
mkdir build; cd build
cmake ..
cmake --build . --config Release
./end_to_end
```